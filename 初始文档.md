# TimeCapsule 技术方案文档

## 1. 项目定位

### 项目名称

TimeCapsule

### 产品定位

一个 AI 驱动的数字记忆空间平台。

用户可以通过上传照片、时间、地点与简单文字，让 AI 自动生成具有情绪表达能力的“回忆卡片”与“记忆故事”。

项目核心不是“记录”，而是：

* 情绪表达
* 记忆整理
* 仪式感
* 沉浸式浏览体验
* AI 辅助 storytelling

---

# 2. 产品目标

## MVP 阶段目标

验证以下核心问题：

> 用户是否会因为 AI 生成的回忆表达而产生情绪共鸣，并愿意分享？

---

## 核心体验

用户上传：

* 图片
* 日期
* 一句话

系统生成：

* AI 回忆文案
* 回忆卡片
* 时间轴内容
* 沉浸式展示页面

---

# 3. 用户场景

## 初始用户群体

* 情侣
* 旅行记录用户
* 毕业纪念
* 家庭回忆
* 宠物记录
* 个人成长记录

情侣只是第一阶段切入点，而非最终产品边界。

---

# 4. MVP 功能范围

## 第一阶段只做以下功能

### 4.1 Landing Page

功能：

* 产品介绍
* AI 回忆卡片展示
* 上传入口
* CTA（Create Your Memory）

设计风格：

* Apple 风格
* 极简
* 毛玻璃
* 动态渐变背景
* 高级感

---

## 4.2 用户登录

### 第一阶段

- Google 登录
- GitHub 登录

### 后续扩展

- 微信登录
- Apple 登录

### JWT 认证策略

**Token 类型：**

| 类型 | 存储位置 | 有效期 | 用途 |
|------|---------|--------|------|
| access_token | 前端内存（变量） | 1 小时 | API 请求认证 |
| refresh_token | httpOnly secure cookie | 30 天 | 无感刷新 access_token |

**为什么不把 access_token 放 cookie：**
- 移动端/React Native 后续扩展时 cookie 不可用
- 前端内存 + Authorization header 是跨端通用方案

**刷新流程：**

```
1. 前端拦截 401 响应（code = 40101）
2. 自动调用 POST /api/v1/auth/refresh（cookie 自动携带 refresh_token）
3. 成功 → 更新内存中的 access_token → 重放原始请求
4. 失败 → 清除状态 → 跳转登录页
```

**前端实现（Next.js middleware 参考）：**

- `middleware.ts` 检查 cookie 中是否有 `refresh_token`
- 无 → 重定向到登录页
- 有 → 放行，由客户端组件负责 access_token 的获取与刷新

### Phase 1 匿名试用方案

MVP 前期可不强制登录，支持匿名试用：

1. 用户访问 Landing Page → 点击 "Try Now"
2. 后端生成一个匿名 session（UUID），返回 `session_token`（httpOnly cookie，7 天有效）
3. 匿名 session 可创建 **1 个 Space、最多 5 条 Memory**
4. 用户后续登录时，通过 `POST /api/v1/auth/claim-session` 将匿名数据迁移到正式账号

> 这样可以在不打断用户体验的前提下，降低试用门槛，同时为后续转化铺路。

---

## 4.3 创建 Memory Space

用户创建一个记忆空间：

示例：

* Tokyo Summer 2026
* Our First Year
* Graduation Day

字段：

* title
* description
* cover image
* theme

---

## 4.4 上传 Memory

用户上传：

* 图片
* 日期
* 一句话描述
* 地点（可选）

系统保存：

* 原图
* 压缩图
* metadata

---

## 4.5 AI 回忆生成（核心）

用户输入：

“第一次一起看海”

AI 输出：

“海风很大，
但从那天开始，
你们开始认真计划未来。”

第一阶段：

* Prompt Engineering
* 单次文本生成

后续阶段：

* 长文本回忆
* AI 总结
* AI 年度回顾
* AI 视频脚本
* AI 时间轴生成

---

## 4.6 沉浸式浏览页

核心体验：

- 卡片切换动画
- 渐变背景
- 音乐氛围
- 时间轴
- 图片沉浸浏览

> 这是产品灵魂。

### 技术实现方案

#### 页面架构

```
SpaceViewPage
├── BackgroundLayer          # 动态渐变/模糊背景
│   ├── 当前卡片的毛玻璃模糊背景（CSS backdrop-filter）
│   └── 多色渐变叠加层
│
├── TimelineIndicator        # 右侧时间轴导航
│   ├── 年份/日期标记点
│   ├── 当前高亮指示器
│   └── 点击跳转
│
├── CardStack                # 中央卡片堆叠
│   ├── 当前卡片（全尺寸）
│   ├── 前后卡片（缩小、半透明）
│   └── 左右滑动切换
│
└── AmbientControls          # 氛围控制（底部浮动）
    ├── 背景音乐开关
    └── 自动播放开关
```

#### 卡片切换动画

使用 **Framer Motion** 实现：

```tsx
// 核心动画变体
variants: {
  enter:  { x: 300, opacity: 0, scale: 0.9 },
  center: { x: 0, opacity: 1, scale: 1 },
  exit:   { x: -300, opacity: 0, scale: 0.9 }
}
// 过渡配置
transition: { type: "spring", stiffness: 300, damping: 30 }
```

交互方式：

- 左右滑动手势（drag="x" + onDragEnd 阈值判断）
- 键盘左右箭头
- 滚轮（特定区域内）
- 移动端触摸滑动

#### 动态渐变背景

```css
/* 根据当前卡片主题色动态变化 */
.space-background {
  background: linear-gradient(
    135deg,
    var(--theme-color-1),
    var(--theme-color-2),
    var(--theme-color-3)
  );
  transition: background 1.2s ease-in-out;
}

.space-background::before {
  /* 毛玻璃模糊层：对卡片图片取色并模糊 */
  backdrop-filter: blur(80px) saturate(1.5);
  -webkit-backdrop-filter: blur(80px) saturate(1.5);
}
```

取色策略：前端用 Canvas 读取卡片封面主色调 → 生成 3 色渐变 → CSS 变量动态注入

#### 时间轴组件

```
|<-- 2024 ---- ● ---- 2025 ---- ● ---- 2026 -->|

                    当前高亮（大圆点 + 发光）
```

- 时间轴自动从 Space 内最早的 memory_date 到最晚的
- 圆点位置按日期比例计算
- 点击/拖动到对应日期，卡堆同步跳转
- 实现：纯 CSS + Framer Motion `useMotionValue` 跟踪当前位置

#### 背景音乐

- MVP：无版权环境音效（雨声、海浪、篝火、钢琴），3-5 首可选
- 静音自动播放（浏览器 autoplay 策略：先 muted，用户手势后 unmute）
- 切换卡片时交叉淡入淡出（crossfade 1.5s）
- 技术：HTML5 `<audio>` + `howler.js`（轻量音频库）

#### 性能考虑

- 卡片图片用缩略图（480px WebP），点击放大再用 1200px
- 只渲染当前卡片 + 前后各 1 张（共 3 张），其余懒加载
- `will-change: transform` 提示 GPU 加速
- 移动端降级：减少模糊半径（80px → 40px），动画简化

---

# 5. 技术栈（2026 AI Native）

## 5.1 前端技术栈

### Framework

* Next.js 15
* React 19
* TypeScript

### UI

* TailwindCSS v4
* shadcn/ui
* Framer Motion
* lucide-react

### 状态管理

第一阶段：

* React Server Components
* useState
* useContext

后续：

* Zustand

---

## 5.2 后端技术栈

### Language

* Go

### Web Framework

* Gin

### ORM

* GORM

### API Style

* RESTful API

后续可扩展：

* GraphQL
* gRPC

---

## 5.3 数据层

### Database

* PostgreSQL

### Cache

后续阶段：

* Redis

### Object Storage

开发阶段：

* Supabase Storage

后续阶段：

* MinIO
* AWS S3
* 腾讯云 COS
* 阿里云 OSS

---

## 5.4 AI 技术栈

### LLM

第一阶段：

* OpenAI API

后续支持：

* Claude API
* Gemini API
* DeepSeek
* Local LLM

### AI SDK

* Vercel AI SDK

### 后续 AI 能力

* Embedding
* RAG
* AI Story Generation
* AI Timeline
* AI Image Understanding

---

## 5.5 Infra

### Monorepo

* Turborepo

### Container

* Docker
* Docker Compose

### Deployment

Frontend:

* Vercel

Backend:

* Railway
* Fly.io

Database:

* Supabase
* Neon

---

## 5.6 图片处理规范

### 上传限制

| 项目 | 限制 |
|------|------|
| 最大文件大小 | 20MB |
| 允许格式 | jpg, jpeg, png, webp, heic |
| 禁止格式 | gif, bmp, tiff, svg |

### 缩略图规格

| 用途 | 尺寸 | 格式 | 质量 | 预计大小 |
|------|------|------|------|----------|
| 列表卡片 | 480px 宽（等比缩放） | WebP | 80% | ~30KB |
| 详情封面 | 1200px 宽（等比缩放） | WebP | 85% | ~150KB |
| 原图 | 保留原始尺寸 | 原始格式 | 100% | 原始大小 |

### 文件命名规范

```
原始文件: {user_id}/{space_id}/{memory_id}/original.{ext}
缩略图:   {user_id}/{space_id}/{memory_id}/thumb_480.webp
封面图:   {user_id}/{space_id}/{memory_id}/cover_1200.webp
```

### 处理流程（Go 后端）

```
1. 接收 multipart 上传
2. 校验 Content-Type（魔数检测，非仅信任扩展名）
3. 校验文件大小
4. 上传原图到 Supabase Storage
5. 用 imaging 库（如 govips 或 bimg）生成缩略图
6. 上传缩略图到 Supabase Storage
7. 写入数据库记录
8. 返回 memory 对象
```

### 前端上传优化

- 上传前用 `canvas` 在前端预压缩（宽 > 1920px 时缩至 1920px）
- 减少上传带宽和用户等待时间
- 上传进度条（XMLHttpRequest + progress 事件）

---

# 6. 项目目录结构

```txt
TimeCapsule/

├── apps/
│   ├── web/              # Next.js Frontend
│   │   ├── src/
│   │   │   ├── app/          # App Router (RSC)
│   │   │   ├── components/   # UI Components
│   │   │   ├── lib/          # Utils, API client
│   │   │   └── styles/       # Global styles
│   │   ├── public/
│   │   ├── next.config.ts
│   │   └── package.json
│   │
│   └── api/              # Go Backend
│       ├── cmd/
│       │   └── server/
│       │       └── main.go       # 入口
│       ├── internal/
│       │   ├── handler/          # HTTP handlers（按领域分文件）
│       │   │   ├── auth.go
│       │   │   ├── space.go
│       │   │   ├── memory.go
│       │   │   └── ai.go
│       │   ├── middleware/
│       │   │   ├── auth.go       # JWT 验证
│       │   │   ├── cors.go
│       │   │   └── ratelimit.go
│       │   ├── model/            # GORM models
│       │   │   ├── user.go
│       │   │   ├── space.go
│       │   │   └── memory.go
│       │   ├── repository/       # 数据访问层
│       │   │   ├── user_repo.go
│       │   │   ├── space_repo.go
│       │   │   └── memory_repo.go
│       │   ├── service/          # 业务逻辑层
│       │   │   ├── auth_service.go
│       │   │   ├── space_service.go
│       │   │   ├── memory_service.go
│       │   │   └── ai_service.go
│       │   ├── dto/              # Request/Response DTOs
│       │   │   ├── request.go
│       │   │   └── response.go
│       │   └── config/
│       │       └── config.go     # 配置加载（env/viper）
│       ├── pkg/                  # 可对外暴露的公共库
│       │   ├── storage/          # 对象存储适配器
│       │   ├── ai/               # AI SDK 封装
│       │   └── imgproc/          # 图片处理
│       ├── migrations/           # SQL 迁移文件
│       │   ├── 001_create_users.up.sql
│       │   ├── 001_create_users.down.sql
│       │   └── ...
│       ├── go.mod
│       ├── go.sum
│       └── Makefile
│
├── packages/
│   ├── ui/               # Shared UI
│   ├── types/            # Shared Types
│   └── utils/            # Shared Utils
│
├── docker/
│   ├── docker-compose.yml
│   ├── Dockerfile.web
│   └── Dockerfile.api
│
├── infra/
│   └── (Terraform / Pulumi 配置，后续阶段)
│
├── docs/
│   └── (设计文档、API 文档等)
│
├── .env.example
├── turbo.json
└── README.md
```

---

# 7. 数据库设计（MVP）

## 设计原则

- 所有表统一带 `id`（UUID v4）、`created_at`、`updated_at`、`deleted_at`
- `deleted_at` 为 NULL 表示未删除，非 NULL 表示软删除时间
- GORM 自动处理软删除逻辑，查询时自动过滤 `deleted_at IS NOT NULL` 的记录
- 主键使用 UUID 而非自增 ID，避免顺序暴露和 ID 冲突

---

## users

```sql
id          UUID PRIMARY KEY DEFAULT gen_random_uuid()
email       VARCHAR(255) UNIQUE NOT NULL
name        VARCHAR(100) NOT NULL
avatar_url  TEXT
provider    VARCHAR(20) NOT NULL   -- 'google' | 'github'
provider_id VARCHAR(255) NOT NULL  -- OAuth user id
created_at  TIMESTAMPTZ DEFAULT NOW()
updated_at  TIMESTAMPTZ DEFAULT NOW()
deleted_at  TIMESTAMPTZ            -- 软删除

-- 索引
CREATE UNIQUE INDEX idx_users_provider ON users(provider, provider_id);
CREATE INDEX idx_users_deleted_at ON users(deleted_at);
```

---

## memory_spaces

```sql
id          UUID PRIMARY KEY DEFAULT gen_random_uuid()
user_id     UUID NOT NULL REFERENCES users(id)
title       VARCHAR(200) NOT NULL
description TEXT
cover_image TEXT
theme       VARCHAR(50) DEFAULT 'default'  -- 'default' | 'warm' | 'ocean' | 'forest' | 'sunset'
created_at  TIMESTAMPTZ DEFAULT NOW()
updated_at  TIMESTAMPTZ DEFAULT NOW()
deleted_at  TIMESTAMPTZ

-- 索引
CREATE INDEX idx_spaces_user_id ON memory_spaces(user_id);
CREATE INDEX idx_spaces_deleted_at ON memory_spaces(deleted_at);
```

---

## memories

```sql
id           UUID PRIMARY KEY DEFAULT gen_random_uuid()
user_id      UUID NOT NULL REFERENCES users(id)      -- 冗余字段，方便按用户维度查询
space_id     UUID NOT NULL REFERENCES memory_spaces(id)
image_url    TEXT NOT NULL                            -- 原图
thumbnail_url TEXT NOT NULL                           -- 压缩图
title        VARCHAR(300) NOT NULL                    -- 用户输入的一句话描述
content      TEXT                                      -- 用户补充文字（可选）
ai_content   TEXT                                      -- AI 生成内容，合并自 ai_cards（1:1 关系无需分表）
ai_model     VARCHAR(50)                               -- 生成时使用的模型
ai_prompt    TEXT                                      -- 生成时使用的 prompt（调试/回溯用）
location     VARCHAR(200)                              -- 地点
memory_date  DATE NOT NULL                             -- 回忆日期
sort_order   INT DEFAULT 0                             -- 同一 Space 下的排序
created_at   TIMESTAMPTZ DEFAULT NOW()
updated_at   TIMESTAMPTZ DEFAULT NOW()
deleted_at   TIMESTAMPTZ

-- 索引
CREATE INDEX idx_memories_user_id ON memories(user_id);
CREATE INDEX idx_memories_space_id ON memories(space_id);
CREATE INDEX idx_memories_memory_date ON memories(space_id, memory_date);
CREATE INDEX idx_memories_deleted_at ON memories(deleted_at);
```

> 设计决策：MVP 阶段 `ai_content` 与 `memories` 是 1:1 关系，直接合并到 `memories` 表减少 JOIN。后续若一个 memory 需要多次 AI 生成，再拆出独立的 `ai_generations` 表。

---

## 全局索引策略

```sql
-- 高频查询：按用户 + 软删除过滤
CREATE INDEX idx_spaces_user_active ON memory_spaces(user_id, created_at DESC) WHERE deleted_at IS NULL;
CREATE INDEX idx_memories_user_active ON memories(user_id, created_at DESC) WHERE deleted_at IS NULL;
```

---

# 8. API 设计（MVP）

## 8.1 统一响应格式

所有 API 返回统一结构：

```json
{
  "code": 0,
  "message": "ok",
  "data": { }
}
```

**code 枚举：**

| code | 含义 |
|------|------|
| 0 | 成功 |
| 40001 | 参数校验失败 |
| 40100 | 未登录 |
| 40101 | Token 过期 |
| 40300 | 无权限 |
| 40400 | 资源不存在 |
| 40900 | 冲突（重复创建等） |
| 41300 | 上传文件过大 |
| 41500 | 不支持的文件类型 |
| 42900 | 请求过于频繁 |
| 50000 | 服务器内部错误 |
| 50001 | AI 生成失败 |

**分页统一格式：**

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": [],
    "total": 42,
    "page": 1,
    "page_size": 20
  }
}
```

---

## 8.2 Auth

```txt
POST   /api/v1/auth/login         # OAuth 回调处理
POST   /api/v1/auth/refresh       # 刷新 token
DELETE /api/v1/auth/logout        # 登出（废弃 token）
GET    /api/v1/auth/me            # 获取当前用户
```

### POST /api/v1/auth/login

```
Content-Type: application/json

Request:
{
  "provider": "google",          // "google" | "github"
  "code": "oauth_authorization_code"
}

Response:
{
  "code": 0,
  "data": {
    "access_token": "eyJ...",
    "refresh_token": "eyJ...",
    "expires_in": 3600,
    "user": {
      "id": "uuid",
      "name": "张三",
      "email": "zhangsan@example.com",
      "avatar_url": "https://..."
    }
  }
}
```

### GET /api/v1/spaces?page=1&page_size=20

```
Response:
{
  "code": 0,
  "data": {
    "items": [
      {
        "id": "uuid",
        "title": "Tokyo Summer 2026",
        "description": "...",
        "cover_image": "https://...",
        "theme": "warm",
        "memory_count": 12,
        "created_at": "2026-05-01T12:00:00Z"
      }
    ],
    "total": 5,
    "page": 1,
    "page_size": 20
  }
}
```

---

## 8.3 Memory Space

```txt
POST   /api/v1/spaces              # 创建
GET    /api/v1/spaces              # 列表（分页）?page=1&page_size=20
GET    /api/v1/spaces/:id          # 详情
PATCH  /api/v1/spaces/:id          # 更新
DELETE /api/v1/spaces/:id          # 软删除
```

---

## 8.4 Memory

### POST /api/v1/memories

上传方式：`multipart/form-data`

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| image | file | 是 | 图片文件，最大 20MB |
| title | string | 是 | 一句话描述，最多 300 字符 |
| content | string | 否 | 补充文字 |
| location | string | 否 | 地点，最多 200 字符 |
| memory_date | string | 是 | 日期，格式 YYYY-MM-DD |
| space_id | string | 是 | 所属 Space ID |
| generate_ai | bool | 否 | 是否同步生成 AI 内容，默认 false |

处理流程：

1. 校验文件类型（仅允许 jpg/jpeg/png/webp/heic）
2. 校验文件大小（≤ 20MB）
3. 上传原图到对象存储
4. 异步生成缩略图（480px 宽，WebP 格式）
5. 写入数据库
6. 若 `generate_ai = true`，调用 AI 生成并更新 `ai_content`

```txt
POST   /api/v1/memories              # 上传记忆（multipart/form-data）
GET    /api/v1/memories/:id          # 详情
GET    /api/v1/memories              # 列表（分页）?space_id=xxx&page=1&page_size=20
PATCH  /api/v1/memories/:id          # 更新文字字段
DELETE /api/v1/memories/:id          # 软删除
```

---

## 8.5 AI

```txt
POST /api/v1/ai/generate-memory      # 生成回忆文案
POST /api/v1/ai/regenerate           # 重新生成（同一 memory）
```

### POST /api/v1/ai/generate-memory

```json
// Request
{
  "text": "第一次一起看海",
  "style": "warm",                   // 可选，风格偏好
  "memory_id": "uuid"                // 可选，若传入则生成后直接更新该 memory
}

// Response
{
  "code": 0,
  "data": {
    "content": "海风很大，但从那天开始，你们开始认真计划未来。",
    "model": "gpt-4o",
    "tokens_used": 42
  }
}
```

### 异步生成策略

推荐使用异步模式（Phase 2 优化）：

1. `POST /memories` 创建记录后立即返回
2. 后台任务队列消费生成请求
3. 前端轮询或 WebSocket 通知生成完成

MVP 阶段可先用同步模式简化实现，但需设置 15s 超时。

---

# 9. UI / UX 设计方向

## 风格关键词

* Apple Style
* Minimal
* Emotional
* Cinematic
* Warm
* Elegant
* Immersive

---

## 推荐配色

* 米白
* 奶灰
* 深蓝
* 星空色
* 毛玻璃透明层

---

## 避免风格

* 粉色爱心泛滥
* 土味情侣 UI
* 复杂按钮
* 信息密度过高

---

# 10. AI Prompt 初版

## System Prompt

```txt
You are an emotional memory storyteller.

Your task is to transform short memory descriptions into warm, cinematic, emotionally resonant reflections.

Style:
- poetic
- concise
- warm
- realistic
- cinematic

Avoid:
- exaggerated romance
- cringe internet language
- overly dramatic wording
```

---

## User Prompt

```txt
Memory:
第一次一起看海
```

---

## Expected Output

```txt
海风很大，
但从那天开始，
你们开始认真计划未来。
```

---

# 11. 开发阶段规划

## Phase 1 — MVP

目标：

* 产品雏形
* AI 回忆卡片
* Landing Page
* 上传与展示

周期：

2~3 周

---

## Phase 2 — 用户系统

加入：

* Auth
* 云存储
* 多 Space
* 时间轴

周期：

2~4 周

---

## Phase 3 — AI 增强

加入：

* AI Timeline
* AI Summary
* AI Year Review
* AI Story

周期：

持续迭代

---

## Phase 4 — 社交与分享

加入：

* 分享链接
* 私密空间
* 双人协作
* Theme Market

---

# 12. 面试价值

该项目可覆盖：

## 前端

* React 19
* Next.js 15
* SSR
* RSC
* 动画
* 响应式
* Tailwind

---

## 后端

* Go
* Gin
* REST API
* JWT
* 文件上传
* 权限设计

---

## 数据库

* PostgreSQL
* Schema Design
* 索引优化

---

## Infra

* Docker
* 部署
* Monorepo
* CI/CD

---

## AI

* Prompt Engineering
* AI Workflow
* Vercel AI SDK
* RAG（后续）

---

# 13. 长期演进方向

## AI 方向

* AI 情书
* AI 回忆视频
* AI 时间轴生成
* AI 关系总结
* AI 图片理解

---

## 技术方向

* Redis
* pgvector
* RAG
* WebSocket
* AI Agent
* Local LLM

---

## C++ 方向（后期）

可加入：

* OpenCV 图片处理
* FFmpeg 视频生成
* llama.cpp 本地模型
* 高性能多媒体服务

---

# 14. 当前阶段最重要原则

不要一开始做：

* 微服务
* Kubernetes
* IM
* 社交系统
* 推荐系统
* 复杂 Agent

当前唯一目标：

> 做出第一个让用户“有感觉”的瞬间。

这是 MVP 成败核心。
